@model List<SimplReaderMVC.Models.Reader.SubscriptionVM>

@{
    Layout = null;
}

@foreach(var subscription in Model)
{
    <li>@Html.ActionLink(subscription.Title, "Index", "Reader", new { feedID = subscription.ID }, new {})</li>
}
